django>=4.2,<5.0
pillow>=10.0
gunicorn>=21.2
whitenoise>=6.6
psycopg2-binary>=2.9
dj-database-url>=2.1

# Supabase (primary storage, auth sync, realtime)
supabase>=2.0.0
realtime>=1.0.0

# Cloudinary (legacy — keep during migration window, remove after)
cloudinary>=1.36.0
django-cloudinary-storage>=0.3.0

# Real-time & WebSockets
channels>=4.0.0
daphne>=4.0.0
channels-redis>=4.1.0
uvicorn[standard]>=0.23.0

# Firebase (push notifications)
firebase-admin>=6.0.0

# Caching
django-redis>=5.4
redis>=5.0

# Auth & Security
python-decouple>=3.8
Django-CORS-headers>=4.3
djangorestframework>=3.14
requests>=2.31
cryptography>=41.0
PyJWT>=2.8.0

# Logging
python-json-logger>=2.0.7

# Utilities
python-dotenv>=1.0.0
